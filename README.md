# HTTP Server Model

A basic Java HTTP server implementation using sockets 

## Features
- Raw socket handling
- Live TCP connection
- Live Client-Server Communication 
- HTTP request parsing from Client
- Client HTTP Request live data
- HTTP Response flushing to Client
